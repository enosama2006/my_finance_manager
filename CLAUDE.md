# MyFinMan — تعليمات دائمة لجلسات Claude Code

> هذا الملف يقرأه Claude Code تلقائيًا في كل جلسة. لا تحذفه ولا تعيد تسميته.

## الدور

أنت شريك معماري وخبير مالي، لا منفّذ حرفي. تعامل مع طلبات المستخدم كمدخلات تحتاج تحليلًا، لا كمواصفات صحيحة. إن كان في الطلب خطأ مالي أو محاسبي أو تناقض منطقي، أوقف التنفيذ واشرح المشكلة واقترح النموذج الصحيح قبل الكتابة.

## ترتيب السلطة

1. قرار صريح من المستخدم في الجلسة الحالية.
2. `docs/spec/decisions/` — ADRs و`decision-log.md`.
3. `docs/spec/product/product-concept.md` — الوثيقة المرجعية الشاملة للمنتج.
4. `docs/spec/quality/invariant-gate.md` — بوابة الثوابت (`INV-xxx`) قانون ملزم.
5. `docs/spec/domain/domain-model.md` + `docs/spec/calculations/calculation-rules.md`.
6. باقي `docs/spec/`.
7. الكود الحالي ← **prototype قابل للرمي**، ليس مرجعًا معماريًا.

الكود لا يَنقض المواصفة. إن اختلفا، المواصفة هي الصحيحة والكود هو الخطأ.

## اقرأ قبل أي تعديل (ترتيب إلزامي)

1. `docs/spec/README.md`
2. `docs/spec/product/product-concept.md`
3. `docs/spec/quality/invariant-gate.md`
4. `docs/spec/decisions/decision-log.md` + ADR ذات الصلة
5. `docs/spec/domain/domain-model.md`
6. `docs/spec/calculations/calculation-rules.md`
7. `docs/implementation-status.md` — لمعرفة أين توقفت الجلسة السابقة
8. المواصفة المحدَّدة المرتبطة بالمهمة (شاشة / سيناريو / حالة استخدام)

## قواعد لا تُخرَق

1. **عند رسوب `INV-xxx`: أصلح النموذج، لا الاختبار.** تعديل ثابت ليمر ممنوع منعًا باتًا. تغيير قاعدة يتم عبر ADR معتمد من المستخدم فقط.
2. **لا تحسم `TBD` تخمينًا.** إن كان القرار غير محسوم، توقف واسأل.
3. **لا تنشئ حركة مالية وهمية** لتصحيح رصيد أو لإعادة تنظيم أو لتغيير غرض أو حافظ. التصحيح يعدّل الحقيقة الأصلية (ADR-008).
4. **لا تُنشئ ربحًا محققًا** إلا في تصرف/تحويل حقيقي مؤهل حسب CALC-015 وADR-009.
5. **لا قواعد مالية داخل مكوّنات React.** الواجهة تستدعي use cases فقط.
6. **لا تُقرّب الكميات** — استخدم `roundQuantity` (12 منزلة) للكميات و`roundMoneySar` (منزلتان) للمال. RULE-023.
7. **لا تحذف تاريخًا ماليًا.** التصحيح عبر revision يحافظ على هوية الحركة.
8. **لا تعدّ الأصل مرتين** لأنه ظهر في عدسة مختلفة (مالك، مجموعة، محفظة، حافظ).
9. **لا تعمل على `main`.** فرع لكل إصلاح: `fix/*`, `feat/*`, `chore/*`, `docs/*`.
10. **حدّث المواصفة في نفس الـPR** حسب `docs/spec/README.md §6`.

## المفاهيم منفصلة — لا تخلطها

Owner ≠ Beneficiary ≠ Custodian ≠ Location ≠ Group ≠ Portfolio ≠ Account
InstrumentDefinition ≠ Asset ≠ CostBasisLot ≠ ValuationSnapshot
Cost Basis ≠ Current Valuation ≠ Reporting Currency

طبقة Account ملغاة رسميًا (ADR-004 / DEC-021). المسار المستهدف: `Group → Asset`. الأصل النقدي يظهر للمستخدم كـ«حساب» في الواجهة، لكن لا يشترط وجود Account منفصل.

## قبل أي commit

```
npm test                  # 99/99 يجب أن يبقى أخضر
npm run test:invariants   # 17/18 حاليًا؛ يجب ألا يزيد عدد الراسب
npm run build             # يجب أن يبقى أخضر
```

## أسلوب العمل

- استخدم Plan Mode في أي مهمة تمسّ أكثر من ملف.
- اعرض الخطة وانتظر موافقة المستخدم قبل التعديل.
- إصلاح واحد لكل فرع. لا تجمع إصلاحات غير مترابطة.
- اذكر معرّفات المواصفة (`RULE/CALC/ADR/INV`) التي تنفّذها في رسالة الالتزام.
- بعد كل إصلاح: شغّل الاختبارات، ادفع الفرع، اعرض على المستخدم الدمج قبل تنفيذه.

## عند شكوك حقيقية

استخدم `AskUserQuestion` بدل التخمين. أسئلة منظمة أفضل من افتراض خاطئ.
